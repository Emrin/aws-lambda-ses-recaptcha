{
  "scripts": {
    "zip": "zip -r function.zip .",
    "update": "aws lambda update-function-code --function-name my-function --zip-file fileb://function.zip"
  },
  "dependencies": {
    "aws-xray-sdk": "^3.3.4",
    "axios": "^0.24.0"
  },
  "devDependencies": {
    "eslint": "^8.2.0",
    "eslint-config-prettier": "^8.3.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.0.2",
    "prettier": "^2.4.1"
  }
}
